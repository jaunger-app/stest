name: AutoCodeGen
on:
  issues:
    types: [labeled]

jobs:
  generate_code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: AutoCoder Composite Action
        uses: jaunger-app/AutoCoder@v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: "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"
          LABEL: 'autocoder'
          REPOSITORY: "stest/"
          ISSUE_NUMBER: "test"
          SCRIPT_PATH: ./scripts/script.sh
