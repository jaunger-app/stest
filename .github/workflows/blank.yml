name: Successful jobs 
on: push
jobs:
  Job1:
    runs-on: windows-latest
    steps:
      - run: echo one
  Job2:
    runs-on: windows-latest
    needs: job1
    if: ${{ false }}
    steps:
      - run: echo two
  Job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - run: echo three
